{
  "name": "cp-nexus-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root for the CP-Nexus monorepo",
  "scripts": {
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "redis": "^5.9.0"
  }
}
